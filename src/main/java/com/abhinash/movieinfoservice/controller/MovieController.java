package com.abhinash.movieinfoservice.controller;import com.abhinash.movieinfoservice.domail.MovieSummary;import com.abhinash.movieinfoservice.repository.MovieSummaryRepository;import com.abhinash.movieinfoservice.service.MovieSummaryService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.web.bind.annotation.*;@RestController@RequestMapping("/movies")public class MovieController {    @Autowired    MovieSummaryService summaryService;    @Autowired    MovieSummaryRepository movieSummaryRepository;    @RequestMapping(value = "/{movieId}", method = RequestMethod.GET)    MovieSummary getMovieInfo(@PathVariable("movieId") Long id) {        return summaryService.getMovieInfo(id);    }    @RequestMapping(value = "/add", method = RequestMethod.POST)    MovieSummary addMovieDetails(@RequestBody MovieSummary movieSummary) {        return movieSummaryRepository.save(movieSummary);    }//    @PutMapping("/updateMovie")//    public MovieSummary updateMovie(@RequestBody MovieSummary MovieSummary) {//        return summaryService.updateMovie(MovieSummary);//    }}